<% if notice %>
  <div id="success-alert" class="alert alert-success" role="alert">
    <%= notice %>
  </div>
  <% end%>

    <div class="container d-flex flex-column justify-content-center border p-4">
      <%= render @complaint %>
        <div>
        <div class="d-flex">

        <%=button_to raw('<i class="fa-solid fa-pencil"></i>'), edit_complaint_path(@complaint),:method =>
              "get",:class=>'btn btn-primary rounded-circle mx-2' %>


          <%= button_to raw('<i class="fa-solid fa-trash-can"></i>'), @complaint, method: :delete,:class=>'btn btn-danger
            rounded-circle mx-2' %>

              <%= button_to raw('<i class="p-1 fa-solid fa-caret-left"></i>'), complaints_path,:method => "get",:class=>'btn
                btn-primary rounded-circle mx-2' %>
              </div>
        </div>

    </div>